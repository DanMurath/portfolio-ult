import ResumeContent from "../components/resume/ResumeContent";

const Resume = () => {
  return (
    <div className="home resume">
      <div className="home-body resume-body">
        <ResumeContent />
      </div>
    </div>
  );
};

export default Resume;
